# CLI Music Player

Playback controls added on top of the terminal UI.

| Key | Action |
| --- | --- |
| `↑` / `↓` | Select track |
| `Enter` | Play highlighted track |
| `Space` / `p` | Pause / resume |
| `←` / `→` | Seek ±5s |
| `m` | Mute |
| `q` | Quit |

```bash
npm start
```
